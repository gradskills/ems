import type { FormDef, FormResponse } from "@/lib/types";

// Mock data removed — forms & responses come from Supabase only.
export const forms: FormDef[] = [];
export const formResponses: FormResponse[] = [];
